// this is our dataStore for js

const dataStore = {
    latitude: "Enter latitude",
    longitude: "Enter longitude",
}; // this is our global data object

export function getData() {
    return dataStore;
}